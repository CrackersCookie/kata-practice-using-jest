const { every, contains, some } = require('../katas/every-contains-some');

describe('every', () => {
  it('', () => {

  });
});

describe('contains', () => {
  it('', () => {

  });
});

describe('some', () => {
  it('', () => {

  });
});